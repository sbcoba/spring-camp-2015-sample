spring:
  application:
    name: springCampClientSample
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:${config.port:8888}
      username: user
      password: password